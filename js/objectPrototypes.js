var game = {
  // Functions save game, load game, start new game
  // Array of players
  // Map object
};

var map = {
  // Array of {room}s
};

var room = {
  // Description
};

var player = {
  // Name, HP, attack power
  // Inventory
  // Position on map
};

var enemy = {
  // Name, type, HP, attack power
  // Inventory
};

var item = {
  // Name, type
};

var inventory = {
  // Array of {item}s
  // Add, Delete, List functions
};

var dictionary = {
  // Array of possible words
  // Lookup function
};
